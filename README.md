# API ENDPOINTS

```bash
# Gateway (pf.conf, relayd.conf)
/gateway?file=*

# Proxmox-dell (storage.cfg, lxc)
/proxmox-dell?file=storage.cfg
/proxmox-dell/lxc?id=*

# Reverse proxy (pf.conf, relayd.conf, httpd.conf)
/reverse-proxy?file=*
```
